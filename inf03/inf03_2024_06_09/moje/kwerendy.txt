kw1
    SELECT w.miejsce, w.liczbaDni FROM wycieczki AS w WHERE w.cena < 1000;
kw2
    SELECT w.liczbaDni, AVG(w.cena) AS `sredniaCena` FROM wycieczki AS w GROUP BY w.liczbaDni;
kw3
    SELECT miejsce, nazwa FROM wycieczki JOIN zdjecia ON wycieczki.id = zdjecia.Wycieczki_id WHERE cena > 500;
kw4 
    CREATE USER 'Ewa'@'localhost' IDENTIFIED BY 'Ewa!Ewa';